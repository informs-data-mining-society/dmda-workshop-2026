---
import {
  ArrowRight,
  ArrowUpRight,
  CalendarDays,
  CheckCircle2,
  FileText,
  History,
  Info,
  MapPin,
  Send
} from 'lucide-astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import DeadlineTimeline from '@/components/DeadlineTimeline.astro';
import pastWorkshops from '@/data/past-workshops.json';
import site from '@/data/site.json';
import organizers from '@/data/organizers.json';
import topics from '@/data/topics.json';

const base = import.meta.env.BASE_URL;
const eventJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Event',
  name: `${site.edition} ${site.name}`,
  description: site.description,
  startDate: site.startDate,
  endDate: site.endDate,
  eventStatus: 'https://schema.org/EventScheduled',
  eventAttendanceMode: 'https://schema.org/OfflineEventAttendanceMode',
  location: {
    '@type': 'Place',
    name: site.venue,
    address: {
      '@type': 'PostalAddress',
      addressLocality: 'San Francisco',
      addressRegion: 'CA',
      addressCountry: 'US'
    }
  },
  image: [`https://informs-data-mining-society.github.io${base}images/san-francisco-hero.png`],
  organizer: {
    '@type': 'Organization',
    name: site.sponsor,
    url: 'https://connect.informs.org/data-mining/home'
  },
  offers: {
    '@type': 'Offer',
    url: site.registrationUrl,
    availability: 'https://schema.org/InStock',
    validThrough: '2026-10-17T23:59:00-04:00'
  }
};
---

<BaseLayout jsonLd={eventJsonLd}>
  <section class="home-hero" id="top" style={`--hero-image: url("${base}images/san-francisco-hero.png")`}>
    <div class="container home-hero__inner">
      <span class="hero-eyebrow">The {site.edition} INFORMS Workshop</span>
      <h1>Data Mining &<br />Decision Analytics</h1>
      <p>A focused day for consequential research at the intersection of data, learning, optimization, and decisions.</p>
      <div class="hero-actions">
        <a class="button button--primary" href={site.submissionUrl} target="_blank" rel="noreferrer">
          <Send size={17} /> Submit a paper <ArrowUpRight size={16} />
        </a>
        <a class="button button--light" href={`${base}#call-for-papers`}>
          Read the call for papers <ArrowRight size={16} />
        </a>
      </div>
    </div>
  </section>

  <section class="event-ribbon" aria-label="Workshop essentials">
    <div class="container event-ribbon__grid">
      <div><CalendarDays size={20} /><span><small>Date</small><strong>{site.date}</strong></span></div>
      <div class="event-location">
        <MapPin size={20} />
        <span>
          <small>Location</small>
          <strong>{site.city}</strong>
          <span class="venue-addresses">Moscone Center South, 747 Howard Street<br />Marriott Marquis San Francisco, 780 Mission Street</span>
        </span>
      </div>
      <div class="deadline-countdown" data-deadline="2026-08-01T23:59:00-04:00">
        <span class="countdown-value" aria-hidden="true">11</span>
        <span><small>Paper deadline</small><strong>August 1 · 11:59 PM ET</strong></span>
      </div>
    </div>
  </section>

  <section class="section section--white intro-section" id="about">
    <div class="container intro-grid">
      <div>
        <span class="kicker">About the workshop</span>
        <h2>From patterns in data to decisions that matter.</h2>
      </div>
      <div class="intro-copy">
        <p>The INFORMS Data Mining Society invites researchers and practitioners to share recent work in data mining, decision analytics, artificial intelligence, machine learning, optimization, and related methodological and applied areas.</p>
        <p>Held the day before the 2026 INFORMS Annual Meeting, DMDA brings together theoretical advances and field-tested applications in a focused, in-person setting.</p>
        <a class="button button--text" href={site.officialUrl} target="_blank" rel="noreferrer">
          View the official INFORMS listing <ArrowUpRight size={16} />
        </a>
      </div>
    </div>
  </section>

  <section class="section deadline-section" id="call-for-papers">
    <div class="container">
      <div class="section-heading">
        <span class="kicker">Call for Papers</span>
        <h2>Submit recent work in data mining and decision analytics.</h2>
        <p>The INFORMS Data Mining Society invites full-paper submissions presenting methodological or applied research in data mining, decision analytics, artificial intelligence, machine learning, optimization, and related fields.</p>
      </div>
      <div class="cfp-grid">
        <article>
          <h3>Paper format</h3>
          <ul class="check-list">
            <li><CheckCircle2 size={18} /><span>Maximum of 10 pages total, including abstract, tables, figures, references, appendices, and all other material.</span></li>
            <li><CheckCircle2 size={18} /><span>One-inch margins, 11-point font or larger, single-column and single-spaced.</span></li>
            <li><CheckCircle2 size={18} /><span>Include a 50-word abstract.</span></li>
            <li><CheckCircle2 size={18} /><span>Prepare both a blinded and an unblinded PDF.</span></li>
          </ul>
        </article>
        <article>
          <h3>Eligibility and review</h3>
          <ul class="plain-list">
            <li>The award nominee or lead author must be a member of the INFORMS Data Mining Society at the time of the 2026 Annual Meeting.</li>
            <li>Submitted work must be neither published nor accepted for publication elsewhere at the time of submission.</li>
            <li>An author may serve as lead author on at most one submission per competition track.</li>
            <li>Review is double-blind. Four finalists will be selected for each track by external referees.</li>
          </ul>
        </article>
      </div>
      <div class="cfp-topics">
        <h3>Topics of interest</h3>
        <ul class="topic-list">
          {topics.map((topic) => <li>{topic}</li>)}
        </ul>
      </div>
      <div class="section-heading section-heading--compact">
        <span class="kicker">Important dates</span>
        <h2>Four dates to keep in view.</h2>
      </div>
      <DeadlineTimeline />
      <div class="deadline-actions">
        <a class="button button--primary" href={site.submissionUrl} target="_blank" rel="noreferrer">
          Submit through Google Forms <ArrowUpRight size={16} />
        </a>
        <a class="button button--secondary" href={site.cfpUrl} target="_blank" rel="noreferrer">
          <FileText size={16} /> View official CFP <ArrowUpRight size={16} />
        </a>
      </div>
    </div>
  </section>

  <section class="section section--white program-section" id="program">
    <div class="container program-shell">
      <div class="section-heading">
        <span class="kicker">Program</span>
        <h2>A one-day program in San Francisco.</h2>
        <p>Session times, speakers, and room assignments will be published as the program is finalized.</p>
      </div>
      <div class="program-notice">
        <Info size={25} />
        <div>
          <h3>The schedule is taking shape.</h3>
          <p>Research talks, invited perspectives, and conversations on data mining and decision analytics will be added here as sessions are confirmed.</p>
        </div>
      </div>
      <div class="program-preview">
        <div class="program-day"><CalendarDays size={19} /><strong>October 31, 2026</strong><span>San Francisco · In person</span></div>
        <div class="program-row"><time>07:30</time><span>Workshop registration and welcome</span><em>Details coming soon</em></div>
        <div class="program-row"><time>09:00</time><span>Research presentations and invited sessions</span><em>Details coming soon</em></div>
        <div class="program-row"><time>12:00</time><span>Lunch and community conversations</span><em>Details coming soon</em></div>
        <div class="program-row"><time>13:30</time><span>Research presentations and panels</span><em>Details coming soon</em></div>
        <div class="program-row"><time>17:00</time><span>Workshop close</span><em>Details coming soon</em></div>
      </div>
    </div>
  </section>

  <section class="section section--white organizer-section" id="organizers">
    <div class="container">
      <div class="organizer-heading">
        <div class="section-heading">
          <span class="kicker">Workshop co-chairs</span>
          <h2>Organized across institutions.</h2>
        </div>
      </div>
      <div class="organizer-row">
        {organizers.map((organizer) => (
          <article class="organizer-mini">
            <span class="organizer-initials" aria-hidden="true">
              {organizer.name.split(' ').map((part) => part[0]).join('')}
            </span>
            <h3>{organizer.name}</h3>
            <a class="organizer-email" href={`mailto:${organizer.email}`}>{organizer.email}</a>
            <p>{organizer.institution}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="section past-section" id="past-workshops">
    <div class="container past-list">
      <div class="section-heading">
        <span class="kicker">Past Workshops</span>
        <h2>DMDA across recent INFORMS Annual Meetings.</h2>
        <p>The workshop is organized annually by the INFORMS Data Mining Society in conjunction with the INFORMS Annual Meeting.</p>
      </div>
      <div class="past-intro"><History size={24} /><p>Use these links to visit previous workshop websites and the current 2026 workshop page.</p></div>
      <a class="past-row past-row--current" href={`${base}#top`}>
        <span class="past-edition">21st</span>
        <span><strong>2026 · San Francisco</strong><small>Current workshop</small></span>
        <ArrowUpRight size={18} />
      </a>
      {pastWorkshops.map((event) => (
        <a class="past-row" href={event.url} target="_blank" rel="noreferrer">
          <span class="past-edition">{event.edition}</span>
          <span><strong>{event.year} · {event.city}</strong><small>INFORMS Workshop on Data Mining & Decision Analytics</small></span>
          <ArrowUpRight size={18} />
        </a>
      ))}
    </div>
  </section>

  <script>
    const countdown = document.querySelector<HTMLElement>('[data-deadline]');
    if (countdown) {
      const deadline = new Date(countdown.dataset.deadline ?? '').getTime();
      const days = Math.max(0, Math.ceil((deadline - Date.now()) / 86_400_000));
      const value = countdown.querySelector<HTMLElement>('.countdown-value');
      if (value) {
        value.textContent = String(days);
        value.setAttribute('aria-label', `${days} days until the paper submission deadline`);
      }
    }
  </script>
</BaseLayout>

<style>
  .home-hero {
    position: relative;
    min-height: min(650px, calc(100svh - 154px));
    display: grid;
    align-items: center;
    overflow: hidden;
    color: var(--white);
    background-color: #1e2b29;
    background-image: var(--hero-image);
    background-position: center;
    background-size: cover;
  }

  .home-hero::before {
    position: absolute;
    inset: 0;
    content: '';
    background: rgba(10, 20, 19, 0.62);
  }

  .home-hero__inner {
    position: relative;
    z-index: 1;
    padding-block: 62px;
  }

  .hero-eyebrow {
    display: block;
    margin-bottom: 17px;
    color: var(--yellow);
    font-size: 13px;
    font-weight: 800;
    text-transform: uppercase;
  }

  .home-hero h1 {
    max-width: 880px;
    margin-bottom: 22px;
    font-size: 76px;
    text-wrap: balance;
  }

  .home-hero p {
    max-width: 670px;
    margin-bottom: 30px;
    color: rgba(255, 253, 250, 0.84);
    font-size: 19px;
  }

  .hero-actions,
  .deadline-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .event-ribbon {
    color: var(--white);
    background: var(--teal-dark);
  }

  .event-ribbon__grid {
    min-height: 104px;
    display: grid;
    grid-template-columns: 0.75fr 1.65fr 0.95fr;
  }

  .event-ribbon__grid > div {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 20px 30px;
    border-right: 1px solid rgba(255, 253, 250, 0.17);
  }

  .event-ribbon__grid > div:first-child {
    padding-left: 0;
  }

  .event-ribbon__grid > div:last-child {
    padding-right: 0;
    border-right: 0;
  }

  .event-ribbon span:not(.countdown-value) {
    display: grid;
  }

  .event-ribbon small {
    color: rgba(255, 253, 250, 0.6);
    font-size: 10px;
    font-weight: 750;
    text-transform: uppercase;
  }

  .event-ribbon strong {
    font-size: 14px;
  }

  .venue-addresses {
    margin-top: 5px;
    color: rgba(255, 253, 250, 0.68);
    font-size: 12px;
    line-height: 1.35;
  }

  .countdown-value {
    min-width: 45px;
    color: var(--yellow);
    font-family: var(--serif);
    font-size: 38px;
    line-height: 1;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 90px;
  }

  .intro-grid h2 {
    margin-bottom: 0;
    font-size: 51px;
  }

  .intro-copy p {
    color: var(--ink-soft);
    font-size: 17px;
  }

  .intro-copy .button {
    margin-top: 9px;
  }

  .deadline-actions {
    margin-top: 30px;
  }

  .section-heading--compact {
    margin-top: 46px;
    margin-bottom: 28px;
  }

  .section-heading--compact h2 {
    margin-bottom: 0;
    font-size: 34px;
  }

  .cfp-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 42px;
    margin-bottom: 10px;
  }

  .cfp-grid article {
    min-width: 0;
    padding-top: 24px;
    border-top: 1px solid var(--line);
  }

  .cfp-grid h3 {
    margin-bottom: 18px;
    font-family: var(--serif);
    font-size: 26px;
    font-weight: 500;
  }

  .check-list,
  .plain-list {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .check-list li {
    display: grid;
    grid-template-columns: 22px 1fr;
    gap: 10px;
    color: var(--ink-soft);
  }

  .check-list li + li,
  .plain-list li + li {
    margin-top: 11px;
  }

  .check-list svg {
    margin-top: 4px;
    color: var(--teal);
  }

  .plain-list li {
    color: var(--ink-soft);
  }

  .cfp-topics {
    margin-top: 42px;
    padding-top: 24px;
    border-top: 1px solid var(--line);
  }

  .cfp-topics h3 {
    margin-bottom: 18px;
    font-family: var(--serif);
    font-size: 26px;
    font-weight: 500;
  }

  .topic-list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 11px 28px;
  }

  .topic-list li {
    color: var(--ink-soft);
    font-size: 14px;
  }

  .program-shell {
    max-width: 920px;
  }

  .program-notice {
    display: grid;
    grid-template-columns: 32px 1fr;
    gap: 16px;
    padding: 28px;
    color: var(--white);
    background: var(--teal-dark);
    border-left: 7px solid var(--yellow);
  }

  .program-notice svg {
    color: var(--yellow);
  }

  .program-notice h3 {
    margin-bottom: 9px;
    font-family: var(--serif);
    font-size: 29px;
    font-weight: 500;
  }

  .program-notice p {
    max-width: 650px;
    margin-bottom: 0;
    color: rgba(255, 253, 250, 0.75);
  }

  .program-preview {
    margin-top: 46px;
    border-top: 1px solid var(--line);
  }

  .program-day {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 18px 0;
    color: var(--teal-dark);
    border-bottom: 3px solid var(--teal-dark);
  }

  .program-day span {
    margin-left: auto;
    color: var(--ink-soft);
    font-size: 13px;
  }

  .program-row {
    min-height: 79px;
    display: grid;
    grid-template-columns: 100px 1fr 155px;
    gap: 20px;
    align-items: center;
    border-bottom: 1px solid var(--line);
  }

  .program-row time {
    color: var(--red);
    font-family: var(--serif);
    font-size: 20px;
  }

  .program-row span {
    font-weight: 650;
  }

  .program-row em {
    color: var(--ink-soft);
    font-size: 12px;
    font-style: normal;
  }

  .organizer-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 28px;
  }

  .organizer-row {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .organizer-mini {
    min-width: 0;
    padding: 30px 24px;
    border-right: 1px solid var(--line);
  }

  .organizer-mini:first-child {
    padding-left: 0;
  }

  .organizer-mini:last-child {
    padding-right: 0;
    border-right: 0;
  }

  .organizer-initials {
    width: 46px;
    height: 46px;
    display: grid;
    place-items: center;
    margin-bottom: 18px;
    color: var(--white);
    background: var(--teal);
    border-radius: 50%;
    font-family: var(--serif);
    font-size: 13px;
  }

  .organizer-mini h3 {
    margin-bottom: 8px;
    font-family: var(--serif);
    font-size: 20px;
    font-weight: 500;
  }

  .organizer-email {
    display: inline-block;
    margin-bottom: 11px;
    color: var(--teal-dark);
    font-size: 13px;
    font-weight: 700;
    overflow-wrap: anywhere;
    text-decoration: underline;
    text-decoration-color: rgba(13, 77, 72, 0.24);
    text-underline-offset: 4px;
  }

  .organizer-mini p {
    margin-bottom: 0;
    color: var(--ink-soft);
    font-size: 13px;
  }

  .past-list {
    max-width: 900px;
  }

  .past-intro {
    max-width: 610px;
    display: grid;
    grid-template-columns: 30px 1fr;
    gap: 14px;
    margin-bottom: 34px;
    color: var(--teal-dark);
  }

  .past-intro p {
    margin-bottom: 0;
    color: var(--ink-soft);
  }

  .past-row {
    min-height: 105px;
    display: grid;
    grid-template-columns: 90px 1fr 24px;
    gap: 20px;
    align-items: center;
    padding: 17px 14px;
    color: var(--ink);
    border-top: 1px solid var(--line);
    text-decoration: none;
  }

  .past-row:last-child {
    border-bottom: 1px solid var(--line);
  }

  .past-row:hover {
    color: var(--teal-dark);
    background: #edf2ed;
  }

  .past-edition {
    color: var(--red);
    font-family: var(--serif);
    font-size: 25px;
  }

  .past-row span:nth-child(2) {
    display: grid;
    gap: 6px;
  }

  .past-row strong {
    font-size: 17px;
  }

  .past-row small {
    color: var(--ink-soft);
    font-size: 12px;
  }

  .past-row--current {
    color: var(--white);
    background: var(--teal-dark);
    border-color: var(--teal-dark);
  }

  .past-row--current .past-edition {
    color: var(--yellow);
  }

  .past-row--current small {
    color: rgba(255, 253, 250, 0.68);
  }

  .past-row--current:hover {
    color: var(--white);
    background: var(--teal);
  }

  @media (max-width: 820px) {
    .home-hero {
      min-height: min(610px, calc(100svh - 150px));
      background-position: 58% center;
    }

    .home-hero h1 {
      font-size: 58px;
    }

    .event-ribbon__grid {
      grid-template-columns: 1fr 1fr;
    }

    .event-ribbon__grid > div {
      padding: 19px 18px;
    }

    .event-ribbon__grid > div:first-child {
      padding-left: 0;
    }

    .event-ribbon__grid > div:nth-child(2) {
      padding-right: 0;
      border-right: 0;
    }

    .deadline-countdown {
      grid-column: 1 / -1;
      padding-left: 0 !important;
      border-top: 1px solid rgba(255, 253, 250, 0.17);
    }

    .intro-grid,
    .cfp-grid {
      grid-template-columns: 1fr;
      gap: 42px;
    }

    .topic-list {
      grid-template-columns: 1fr;
    }

    .organizer-row {
      grid-template-columns: 1fr 1fr;
    }

    .organizer-mini:nth-child(2) {
      border-right: 0;
    }

    .organizer-mini:nth-child(-n + 2) {
      border-bottom: 1px solid var(--line);
    }

    .organizer-mini:first-child,
    .organizer-mini:last-child {
      padding: 30px 24px;
    }
  }

  @media (max-width: 560px) {
    .home-hero {
      min-height: min(590px, calc(100svh - 142px));
    }

    .home-hero__inner {
      padding-block: 46px;
    }

    .home-hero h1 {
      font-size: 43px;
      overflow-wrap: anywhere;
    }

    .home-hero p {
      font-size: 16px;
    }

    .hero-actions {
      align-items: stretch;
      flex-direction: column;
    }

    .hero-actions .button {
      width: 100%;
    }

    .event-ribbon__grid {
      display: block;
      padding-block: 9px;
    }

    .event-ribbon__grid > div,
    .event-ribbon__grid > div:first-child,
    .event-ribbon__grid > div:last-child {
      min-height: 71px;
      padding: 12px 0;
      border-right: 0;
      border-bottom: 1px solid rgba(255, 253, 250, 0.17);
    }

    .event-ribbon__grid > div:last-child {
      border-bottom: 0;
    }

    .intro-grid h2 {
      font-size: 38px;
    }

    .program-notice {
      padding: 22px;
    }

    .program-day {
      align-items: flex-start;
      flex-wrap: wrap;
    }

    .program-day span {
      width: 100%;
      margin-left: 31px;
    }

    .program-row {
      grid-template-columns: 68px 1fr;
      gap: 8px 15px;
      padding: 14px 0;
    }

    .program-row em {
      grid-column: 2;
    }

    .past-row {
      grid-template-columns: 60px 1fr 20px;
      gap: 12px;
    }

    .organizer-heading {
      align-items: flex-start;
      flex-direction: column;
    }

    .organizer-row {
      grid-template-columns: 1fr;
    }

    .organizer-mini,
    .organizer-mini:first-child,
    .organizer-mini:last-child,
    .organizer-mini:nth-child(2) {
      padding: 24px 0;
      border-right: 0;
      border-bottom: 1px solid var(--line);
    }

    .organizer-mini:last-child {
      border-bottom: 0;
    }
  }
</style>
